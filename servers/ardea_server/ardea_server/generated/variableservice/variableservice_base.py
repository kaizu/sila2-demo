# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .variableservice_types import (
    GetVariableNames_Responses,
    ReadVariable_Responses,
    VariableDataType,
    WriteVariable_Responses,
)

if TYPE_CHECKING:

    from ...server import Server


class VariableServiceBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Read and write controller variables (including I/O) on a DENSO robot controller
        over b-CAP, and list available controller variable names. The server is stateless:
        each command opens a b-CAP connection, acquires the variable handle, performs the
        operation, then releases the handle and disconnects.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def ReadVariable(self, VariableName: str, *, metadata: MetadataDict) -> ReadVariable_Responses:
        """
        Read the value of a controller variable by name.


        :param VariableName: The name of the controller variable to read.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - Value: The variable value, represented as a string.

            - DataType: The detected data type of the value.


        """

    @abstractmethod
    def WriteVariable(
        self, VariableName: str, Value: str, DataType: VariableDataType, *, metadata: MetadataDict
    ) -> WriteVariable_Responses:
        """
        Write a value to a controller variable by name.


        :param VariableName: The name of the controller variable to write.

        :param Value: The value to write, represented as a string.

        :param DataType: The data type used to convert the string value before writing.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def GetVariableNames(self, *, metadata: MetadataDict) -> GetVariableNames_Responses:
        """
        List the names of all available controller variables.


        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - VariableNames: The list of controller variable names.


        """
