# Generated by sila2.code_generator; sila2.__version__: 0.14.0
# -----
# This class does not do anything useful at runtime. Its only purpose is to provide type annotations.
# Since sphinx does not support .pyi files (yet?), this is a .py file.
# -----

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:

    from typing import Iterable, Optional

    from sila2.client import ClientMetadataInstance
    from variableservice_types import GetVariableNames_Responses, ReadVariable_Responses, WriteVariable_Responses

    from .variableservice_types import VariableDataType


class VariableServiceClient:
    """

    Read and write controller variables (including I/O) on a DENSO robot controller
    over b-CAP, and list available controller variable names. The server is stateless:
    each command opens a b-CAP connection, acquires the variable handle, performs the
    operation, then releases the handle and disconnects.

    """

    def ReadVariable(
        self, VariableName: str, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> ReadVariable_Responses:
        """
        Read the value of a controller variable by name.
        """
        ...

    def WriteVariable(
        self,
        VariableName: str,
        Value: str,
        DataType: VariableDataType,
        *,
        metadata: Optional[Iterable[ClientMetadataInstance]] = None,
    ) -> WriteVariable_Responses:
        """
        Write a value to a controller variable by name.
        """
        ...

    def GetVariableNames(
        self, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> GetVariableNames_Responses:
        """
        List the names of all available controller variables.
        """
        ...
