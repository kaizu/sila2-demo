# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .deviceservice_types import ReadDevice_Responses, WriteDevice_Responses

if TYPE_CHECKING:

    from ...server import Server


class DeviceServiceBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Read and write a single PLC device word over the KEYENCE KV COM+ Library
        (DBCommManager.ReadDevice / WriteDevice). The server is stateless: each
        command opens a KV COM+ connection, performs the one-word operation, then
        disconnects. Device type codes and device numbers are passed through to the
        library unchanged; this feature is signal-agnostic and assigns no meaning to
        particular addresses. Multi-word (e.g. 32-bit) values are reconstructed by
        the client from consecutive single-word reads/writes.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def ReadDevice(self, DeviceType: int, DeviceNumber: str, *, metadata: MetadataDict) -> ReadDevice_Responses:
        """

        Read one device word via DBCommManager.ReadDevice(DeviceType, DeviceNumber).
        Returns the raw word value as an integer.



          :param DeviceType: The KV COM+ device type code (e.g. 18 = DM for KV-8000).

          :param DeviceNumber: The device number, passed to the library as a string (e.g. "6010").

          :param metadata: The SiLA Client Metadata attached to the call

          :return:

              - Value: The word value returned by ReadDevice.


        """

    @abstractmethod
    def WriteDevice(
        self, DeviceType: int, DeviceNumber: str, Value: int, *, metadata: MetadataDict
    ) -> WriteDevice_Responses:
        """

        Write one device word via DBCommManager.WriteDevice(DeviceType, DeviceNumber, Value).



          :param DeviceType: The KV COM+ device type code (e.g. 18 = DM for KV-8000).

          :param DeviceNumber: The device number, passed to the library as a string (e.g. "5010").

          :param Value: The word value to write.

          :param metadata: The SiLA Client Metadata attached to the call

        """
