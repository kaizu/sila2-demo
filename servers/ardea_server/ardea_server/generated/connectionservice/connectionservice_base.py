# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .connectionservice_types import GetMode_Responses

if TYPE_CHECKING:

    from ...server import Server


class ConnectionServiceBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Check connectivity to the PLC over the KEYENCE KV COM+ Library. The server is
        stateless: each command opens a KV COM+ connection, queries status, then
        disconnects. Connection parameters come from the server configuration, not
        from command parameters.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def GetMode(self, *, metadata: MetadataDict) -> GetMode_Responses:
        """

        Connect to the PLC and return its operating mode via DBCommManager.GetMode
        (RUN or PROGRAM). A successful response also confirms that the PLC is
        reachable and the connection became active.



          :param metadata: The SiLA Client Metadata attached to the call

          :return:

              - Mode: The PLC operating mode.


        """
