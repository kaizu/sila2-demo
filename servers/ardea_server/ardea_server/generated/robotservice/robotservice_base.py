# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .robotservice_types import GetCartesianPosition_Responses, GetJointAngles_Responses

if TYPE_CHECKING:

    from ...server import Server


class RobotServiceBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Read the current pose of a DENSO robot over b-CAP. The server is stateless:
        each command opens a b-CAP connection, acquires the robot, runs the pose
        command (robot_execute "CurJnt" / "CurPos"), then releases the robot and
        disconnects. The robot is selected as the first robot reported by the
        controller. These commands only read; they do not move the robot.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def GetJointAngles(self, *, metadata: MetadataDict) -> GetJointAngles_Responses:
        """

        Get the current joint angles via robot_execute(robot, "CurJnt"). Returns
        the J-type value as a list of reals (J1..J6 in degrees, plus any auxiliary
        axes).



          :param metadata: The SiLA Client Metadata attached to the call

          :return:

              - JointAngles: The current joint angles, in the order returned by CurJnt.


        """

    @abstractmethod
    def GetCartesianPosition(self, *, metadata: MetadataDict) -> GetCartesianPosition_Responses:
        """

        Get the current Cartesian position via robot_execute(robot, "CurPos").
        Returns the P-type value as a list of reals (X, Y, Z in mm; RX, RY, RZ in
        degrees; the trailing figure value as a number).



          :param metadata: The SiLA Client Metadata attached to the call

          :return:

              - Position: The current Cartesian position, in the order returned by CurPos.


        """
