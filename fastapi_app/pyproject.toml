[project]
name = "fastapi-app"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sila2",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
